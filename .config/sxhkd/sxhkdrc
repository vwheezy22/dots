## System Settings
# Reboot
super + shift + Home
    dmenuprompt "reboot?" "reboot"

# Shutdown
super + shift + End
    dmenuprompt "shutdown?" "shutdown now"

# Screen Lock
Scroll_Lock
    dmenuprompt "lock?" slock

# Volume Control
XF86Audio{LowerVolume,RaiseVolume,Mute}
    pactl set-sink-{volume @DEFAULT_SINK@ -5%,volume @DEFAULT_SINK@ +5%,mute @DEFAULT_SINK@ toggle}



## Programs
# Terminal
ctrl + space
    $TERMINAL

# Mail
super + e
    $TERMINAL neomutt

# Browser
super + w
    $BROWSER

super + shift + w
    firefox --ProfileManager

# Screenshot
ctrl + super + p
    maim -s ~/pics/$(date "+%H.%M_%d-%m-%y").png
